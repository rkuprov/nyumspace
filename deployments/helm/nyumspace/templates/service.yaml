apiVersion: v1
kind: Service
metadata:
  name: nyumspace
  namespace: nyumspace
spec:
  type: {{ .Values.service.type }}
  selector:
    app: nyumspace
  ports:
    - port: {{ .Values.service.port }}
      targetPort: {{ .Values.service.port }}
