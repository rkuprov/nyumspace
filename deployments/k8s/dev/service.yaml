apiVersion: v1
kind: Service
metadata:
  name: nyumspace-api
spec:
  type: NodePort
  selector:
    app: nyumspace-api
  ports:
    - name: nyumspace-api
      port: 8080
      targetPort: 8080
      nodePort: 30080